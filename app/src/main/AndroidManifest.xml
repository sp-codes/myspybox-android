<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="de.sp_codes.myspybox_android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.setup.SetupActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".app.setup.ExplanationActivity"
            android:label="@string/what_is_myspybox" />
        <activity
            android:name=".app.setup.SetupServerActivity"
            android:label="@string/title_activity_setup_server" />
        <activity
            android:name=".app.setup.SetupEncryptionKeyActivity"
            android:label="@string/title_activity_setup_encryption" />
        <activity
            android:name=".app.setup.SetupPermissionsActivity"
            android:label="@string/title_activity_setup_permissions" />

        <service android:name=".service.AppUsageTrackingService" />

        <receiver android:name=".service.MainServicesStarter">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>